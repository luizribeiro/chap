[package]
name = "sage-sdk-provider-fixture"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
sage-plugin = { workspace = true, default-features = false, features = ["provider"] }
