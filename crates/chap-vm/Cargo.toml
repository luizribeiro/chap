[package]
name = "chap-vm"
version.workspace = true
edition.workspace = true
publish.workspace = true

[features]
host = ["dep:serde", "dep:sha2"]
mock = ["host"]

[dependencies]
lockgate-policy.workspace = true
serde = { workspace = true, optional = true }
sha2 = { workspace = true, optional = true }
