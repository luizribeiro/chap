package chap:agent@0.3.0;

interface provider {
  use types.{completion, completion-request, provider-error};

  complete: async func(request: completion-request) -> result<completion, provider-error>;
}
